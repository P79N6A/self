{
    "shell_cmd" : "gcc $file_name -o ${file_base_name} && start cmd /c \" ${file_path}/${file_base_name} & pause \" ",
    "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
    "working_dir": "${file_path}",
    "variants":
    [
        {
            "name": "Run",
            "shell_cmd": "gcc $file_name -o ${file_base_name} && start cmd /c \" ${file_path}/${file_base_name} & pause \" "
        }
    ]

}
